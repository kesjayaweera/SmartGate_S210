services:
  postgres:
    image: postgres:latest   # Use the latest PostgreSQL image
    environment:
      POSTGRES_USER: admin     # Define your database username
      POSTGRES_PASSWORD: smartgate  # Define your database password
      POSTGRES_DB: smartgatedb  # Define your database name
    volumes:
      - smartgate_data:/var/lib/postgresql/data  # Data Storage
      - ./user_schema.sql:/docker-entrypoint-initdb.d/user_schema.sql  # Mount user_schema.sql to be executed at startup
    ports:
      - "5432:5432"  # Expose the port to your host machine

volumes:
  smartgate_data: {}
